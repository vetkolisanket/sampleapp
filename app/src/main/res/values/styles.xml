<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Button">
    </style>

    <style name="Button.Onboarding">
        <item name="android:background">@drawable/bg_white_black_stroke</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="Button.LoginSignUp">
        <item name="android:background">@color/color_accent</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Button.Default">
        <item name="android:background">@drawable/bg_holo_red_light_rounded</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingLeft">@dimen/btn_default_left_right_padding</item>
        <item name="android:paddingRight">@dimen/btn_default_left_right_padding</item>
    </style>

    <style name="Button.Dialog">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@color/color_accent</item>
    </style>

    <style name="Text"></style>

    <style name="Text.Title">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/text_style_large</item>
    </style>

    <style name="Text.Description">
        <item name="android:textColor">@android:color/black</item>
    </style>

</resources>
